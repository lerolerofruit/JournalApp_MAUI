<Window x:Class="JournalApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="My Personal Journal" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">
    
    <Window.Resources>
        <!-- Light Theme -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#2C3E50"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3498DB"/>
        <SolidColorBrush x:Key="TextBrush" Color="#2C3E50"/>
        <SolidColorBrush x:Key="LightTextBrush" Color="White"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#BDC3C7"/>
        <SolidColorBrush x:Key="CardBrush" Color="White"/>
        
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#34495E"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <StackPanel>
                <TextBlock Text="My Journal" 
                          FontSize="20" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          Margin="15,20,15,30"
                          HorizontalAlignment="Center"/>

                <Button x:Name="btnDashboard" Content="📊 Dashboard" Style="{StaticResource SidebarButtonStyle}" Click="BtnDashboard_Click"/>
                <Button x:Name="btnNewEntry" Content="✏️ New Entry" Style="{StaticResource SidebarButtonStyle}" Click="BtnNewEntry_Click"/>
                <Button x:Name="btnCalendar" Content="📅 Calendar" Style="{StaticResource SidebarButtonStyle}" Click="BtnCalendar_Click"/>
                <Button x:Name="btnTimeline" Content="📝 Timeline" Style="{StaticResource SidebarButtonStyle}" Click="BtnTimeline_Click"/>
                <Button x:Name="btnSearch" Content="🔍 Search" Style="{StaticResource SidebarButtonStyle}" Click="BtnSearch_Click"/>
                <Button x:Name="btnAnalytics" Content="📈 Analytics" Style="{StaticResource SidebarButtonStyle}" Click="BtnAnalytics_Click"/>
                <Button x:Name="btnExport" Content="📄 Export PDF" Style="{StaticResource SidebarButtonStyle}" Click="BtnExport_Click"/>
                <Button x:Name="btnSettings" Content="⚙️ Settings" Style="{StaticResource SidebarButtonStyle}" Click="BtnSettings_Click"/>
                <Button x:Name="btnLogout" Content="🚪 Logout" Style="{StaticResource SidebarButtonStyle}" Click="BtnLogout_Click" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
